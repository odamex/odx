# GitHub Personal Access Token for API requests
# This increases the rate limit from 60 to 5000 requests per hour
# Get your token from: https://github.com/settings/tokens
# Scope needed: public_repo (read-only access to public repositories)
# 
# For Development:
# - Copy this file to .env and add your token
# 
# For Production Builds:
# - Set ODX_GITHUB_TOKEN environment variable before running package command
# - Example: ODX_GITHUB_TOKEN=your_token npm run package
# - Or use CI/CD secrets to inject it during build
ODX_GITHUB_TOKEN=your_token_here
